server:
  port: 8081

management:
  endpoints:
    web:
      base-path: /management
      exposure:
        include: ["configprops", "env", "health", "info", "logfile", "loggers", "threaddump"]

spring:
  application:
    name: auth-service

logging:
  level:
    ROOT: INFO
    za.co.sfh.auth: INFO

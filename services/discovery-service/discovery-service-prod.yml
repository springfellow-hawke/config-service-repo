server:
  port: 8761

management:
  endpoints:
    web:
      base-path: /management
      exposure:
        include: ["configprops", "env", "health", "info", "logfile", "loggers", "threaddump"]

spring:
  application:
    name: discovery-service

logging:
  level:
    ROOT: INFO
    za.co.sfh.discovery: INFO
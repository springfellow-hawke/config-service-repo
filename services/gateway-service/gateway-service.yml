server:
  port: 8080

management:
  endpoints:
    web:
      base-path: /management
      exposure:
        include: ["configprops", "env", "health", "info", "logfile", "loggers", "threaddump"]

spring:
  application:
    name: gateway-service

logging:
  level:
    ROOT: INFO
    za.co.sfh.gateway: DEBUG

application:
  test1: Test 1 val
  test2: test 2 value
  test3: Test 3 value